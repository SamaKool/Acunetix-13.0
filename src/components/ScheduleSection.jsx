import React from 'react';

const ScheduleSection = () => (
	<section style={{ padding: '2rem', textAlign: 'center', color: '#fff' }}>
		<h2>Schedule Section</h2>
		<p>This is a placeholder for the event schedule.</p>
	</section>
);

export default ScheduleSection;
